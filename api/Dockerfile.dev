FROM mcr.microsoft.com/dotnet/sdk:10.0

RUN apt-get update \
    && apt-get install -y --no-install-recommends curl \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY . .

EXPOSE 8080
CMD ["dotnet", "watch", "run", "--project", "src/Api.Web/Api.Web.csproj", "--urls", "http://0.0.0.0:8080"]
